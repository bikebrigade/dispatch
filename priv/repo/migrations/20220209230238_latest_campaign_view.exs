defmodule BikeBrigade.Repo.Migrations.LatestCampaignView do
  use Ecto.Migration
  # import BikeBrigade.MigrationUtils

  def change do
    # load_sql(
    #   "programs_latest_campaigns_view.sql",
    #   "drop view if exists programs_latest_campaigns"
    # )
  end
end
